{
  "name": "codealpha-chatbot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CodeAlpha Cloud Computing Task 4 - free retrieval-based commercial support chatbot",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "static-check": "node scripts/static-check.mjs",
    "check": "npm run test && npm run static-check",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
